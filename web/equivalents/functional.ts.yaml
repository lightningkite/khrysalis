---
- id: kotlin.let
  type: call
  template: "(~0~)(~this~)"

- id: kotlin.run
  type: call
  template: "(~0~)(~this~)"

- id: kotlin.with
  type: call
  template: "(~1~)(~0~)"

- id: kotlin.also
  type: call
  template:
    pattern: "also(~this~, ~0~)"
    imports:
      also: khrysalis/dist/Kotlin

- id: kotlin.apply
  type: call
  template:
    pattern: "also(~this~, ~0~)"
    imports:
      also: khrysalis/dist/Kotlin

- id: kotlin.takeIf
  type: call
  template:
    pattern: takeIf(~this~, ~0~)
    imports:
      takeIf: khrysalis/dist/Kotlin

- id: kotlin.takeUnless
  type: call
  template:
    pattern: takeUnless(~this~, ~0~)
    imports:
      takeUnless: khrysalis/dist/Kotlin