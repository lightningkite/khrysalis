import { CanvasGradientMaker, Shader } from "./LinearGradient.actual";
export declare function newRadialGradient(centerX: number, centerY: number, radius: number, colors: Array<string>, positions: Array<number>, tile: Shader.TileMode): CanvasGradientMaker;
