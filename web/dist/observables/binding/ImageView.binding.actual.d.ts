import { Image } from '../../Image.shared';
import { ObservableProperty } from '../ObservableProperty.shared';
export declare function androidWidgetImageViewBindImage(this_: HTMLImageElement, image: ObservableProperty<(Image | null)>): void;
