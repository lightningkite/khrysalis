package com.test


fun headers(): Map<String, String> {
    mapOf<String, String>(
        "Authorization" to "Token $key"
    )
    mapOf(
        "Authorization" to "Token $key"
    )
    return mapOf<String, String>(
        "Authorization" to "Token $key"
    )
    return mapOf(
        "Authorization" to "Token $key"
    )
}
