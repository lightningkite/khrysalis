// Generated by Khrysalis TypeScript converter
// File: /home/josephivie/IdeaProjects/khrysalis/kotlin-compiler-plugin-typescript/testData/testCollections.shared.kt
// Package: com.test.collections
// Imported FQ name: com.test.collections.test.<anonymous>.it TS it
// Imported FQ name: com.test.collections.test.list TS list
// Imported FQ name: com.test.collections.test.mutableList TS mutableList
// Imported FQ name: com.test.collections.test.pair TS pair
// Imported FQ name: com.test.collections.test.x TS x
import { listFilterNotNull } from 'khrysalis/dist/Kotlin'

export function test(){
    const list = [1, 2, 3];
    
    console.log(list[0]);
    
    const pair: [number, string] = [3, "Three"];
    
    console.log(pair[0]);
    console.log(pair[1]);
    
    console.log(list.map((it) => it + 1).filter((it) => it % 2 === 0));
    console.log((list[0] ?? null));
    console.log((list.length == 1 ? list[0] : null));
    
    listFilterNotNull([1, 2, null, 4, 8]).forEach((it) => console.log(it + 3));
    
    const mutableList: Array<number> = [];
    
    mutableList.push(1);
    mutableList.push(2);
    mutableList.splice(0, 0, 2);
    const x = 0;
    
    const index0 = x + 1;
    mutableList[index0] = mutableList[index0] + 4;
    mutableList[0] = mutableList[0] + 4;
    console.log(mutableList[0]);
}
